N = int(input("Введіть натуральне число N (1 ? N ? 100): "))
if 1 <= N <= 100:
    sum_of_numbers = sum(range(1, N + 1))
    print(f"Сума перших {N} натуральних чисел: {sum_of_numbers}")
else:
    print("Невірне введення. Введіть натуральне число в межах від 1 до 100.")
