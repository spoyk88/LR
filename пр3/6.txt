def find_longest_word(sentence):
    words = sentence.split()  
    longest_word = max(words, key=len)  
    return longest_word

input_string = "Добрий ранок"
longest_word = find_longest_word(input_string)
print(f"Найдовше слово у рядку: {longest_word}")
